name:                sub1
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

executables:
  sub1:
    main: Main.hs
    source-dirs: app

tests:
  sub1-spec:
    main:
      Spec.hs
    source-dirs:
    - test
